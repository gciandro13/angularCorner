import { FirstComponentExpPipePipe } from './first-component-exp-pipe.pipe';

describe('FirstComponentExpPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FirstComponentExpPipePipe();
    expect(pipe).toBeTruthy();
  });
});
